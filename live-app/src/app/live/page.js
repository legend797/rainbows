'use server';
import Live from "./Live";


export default async function LivePage({ params }) {
	
	// const { slug } = params;
	// const data = await getData(slug);
	// console.log(data)

	return <Live />;
}
